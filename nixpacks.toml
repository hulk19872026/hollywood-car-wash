providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm ci --omit=dev || npm install --omit=dev"]

[start]
cmd = "node project/backend/server.js"

[variables]
NODE_ENV = "production"
